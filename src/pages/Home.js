import Cards from "../components/Cards"
import Input from "../components/Input"

const Home = (props) => {
    return (
        <div>
            <Input/>
            <Cards/>
        </div>
    )
}
export default Home